// Placeholder App component for extension project
// The main application logic is in the Chrome extension files

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      <div className="text-center p-8">
        <h1 className="text-2xl font-bold mb-4">ATS CV Tailor Extension</h1>
        <p className="text-muted-foreground">
          This project contains a Chrome extension. Load the extension folder in Chrome to use it.
        </p>
      </div>
    </div>
  );
}
